@model IEnumerable<WebApplication8.Models.Dersler>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DersAd)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DersAkts)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DersSaat)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DersAd)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DersAkts)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DersSaat)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DersID }) |
            @Html.ActionLink("Details", "Details", new { id=item.DersID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DersID })
        </td>
    </tr>
}

</table>
